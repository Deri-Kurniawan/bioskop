<nav class="navbar navbar-expand-md navbar-dark bg-dark sticky-top">
    <div class="container">
        <a class="navbar-brand" href="/">Bioskop</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link <%= (title.toLowerCase() === 'home'.toLowerCase()) ? 'active' : '' %>"
                        aria-current="page" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= (title.toLowerCase() === 'movie'.toLowerCase()) ? 'active' : '' %>"
                        href="/movie">Movie</a>
                </li>
            </ul>
            <div class="d-flex">
                <a href="/movie" class="btn btn-outline-light" type="submit"><i class="bi bi-search"></i></a>
            </div>
        </div>
    </div>
</nav>